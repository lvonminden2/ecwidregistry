{
  "name": "gift-registry-section",
  "version": "0.0.8",
  "private": true,
  "type": "module",
  "scripts": {
    "generate-config": "node generate-config.js",
    "dev":    "node generate-config.js && npx @lightspeed/crane dev",
    "build":  "node generate-config.js && npx @lightspeed/crane build",
    "deploy": "node generate-config.js && npx @lightspeed/crane deploy"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@lightspeed/crane": "3.1.0",
    "@lightspeed/eslint-config-crane": "latest",
    "dotenv": "^16.0.0",
    "sass": "^1.0.0",
    "vite": "^5.0.0"
  }
}
